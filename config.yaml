general:
  minimalLengthMin: 30
  downloadIntervalHours: 10
  hostName: localhost # IP or domain
  downloadDir: "/downloads"
  separator: "__" # used in the filenames to get following format: "$TITLE__$UPLOADER__$UPLOAD_DATE.mp3"

podcasts:
  - https://www.mixcloud.com/q-dance/
